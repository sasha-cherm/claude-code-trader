# Session 197: March 31, 2026, 06:26 UTC

## Balance: $14.76

## What happened
Continued from previous session that ran out of context. The main pending task was wiring `track_candle_tokens()` into `run_candle()` — it was defined but never called, making the orphan scanner do nothing.

## Fixes applied this session
1. **Wired `track_candle_tokens(up_token, dn_token)` into `run_candle()`** — called right after token extraction. Now the orphan scanner actually checks recent candle tokens for stuck positions.

## Fixes applied in previous session (carried forward)
- `place_order`: SELL orders no longer inflated to MIN_SIZE (was causing "not enough balance")
- `find_support_price`: BUY placed behind fattest bid wall in top 4 depth levels
- Sell floor: never sell below buy_fill_px + TICK with GTC limit order
- Averaging: when price drops 3+ cents, buy more at bid, sell combined at avg price
- Pre-SELL: cancel ALL token orders before placing SELL (prevents BUY+SELL simultaneously)
- SELL failure: cancel blocking orders before retry
- Partial SELL: keep order alive, don't cancel
- `close_position()`: GTC at best_bid (instant fill) -> FOK fallback -> settlement wait
- Orphan scanner: uses tracked token list instead of Gamma API (was causing 22-min hangs)
- Side-switching: 20% hysteresis + 10-tick evaluation interval

## Observed candle (2:30-2:45 AM ET)
- BUY DN @ 0.45, SELL placed @ 0.46 (sell floor working correctly)
- SELL didn't fill before deadline (market dropped to 0.44/0.45 near end)
- Market sold at deadline: 6sh matched at ~0.45, position fully closed
- Profit: +$0.06 (tiny but positive, and position properly closed)

## Recent trade log analysis (last 5 candles before this session)
- Some candles showed sells below buy price (before sell-floor fix)
- Market sell failures (resp=None) on some candles
- Averaging kicked in on 2 candles (1:00 and 1:15 AM)
- Net: mixed results, but position closure now more reliable

## Status
- Bot running (PID 90955), waiting for next candle
- All major fixes in place
- Key risk remaining: 1-cent spread means profit per candle is tiny ($0.06 on 6 shares), need consistent fills and zero stuck positions to be profitable
